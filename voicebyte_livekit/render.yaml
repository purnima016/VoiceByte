services:
  - type: web
    name: voicebyte
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python backend/app.py
    envVars:
      - key: GROQ_API_KEY
        sync: false
      - key: FAST2SMS_KEY
        sync: false